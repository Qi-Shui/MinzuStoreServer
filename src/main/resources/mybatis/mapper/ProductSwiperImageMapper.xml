<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minzu.minzustore.mapper.ProductSwiperImageMapper">

    <resultMap id="productSwiperTypeResult" type="com.minzu.minzustore.entity.ProductSwiperImage"></resultMap>

    <select id="maxId" parameterType="Integer" resultType="Int">
        select MAX(id) from product_swiper_image
    </select>

</mapper>
